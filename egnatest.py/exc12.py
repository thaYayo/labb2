# Given a tuple, use tuple unpacking to assign each value to a separate variable.

tuples = ("banana", "orange", "cherry")

first, second, third = tuples[0], tuples[1], tuples[2]
print(first, second, third)

